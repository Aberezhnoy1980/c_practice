# C Project Template

Профессиональный шаблон для C проектов с системой сборки, тестированием и документацией.

## 🏗️ Структура проекта

```text
c_project_template/
    ├── src/     # Исходные файлы (.c)
    ├── include/ # Заголовочные файлы (.h)
    ├── out/     # Скомпилированные файлы
    ├── build/   # Объектные файлы (.o)
    ├── tests/   # Тесты
    └── docs/    # Документация
```


## 🚀 Быстрый старт

```bash
# Клонировать или создать проект
git clone <repository>
cd c_project_template

# Сборка проекта
make build

# Запуск
make run

# Тестирование
make test

# Очистка
make clean

📝 Задача
Дано натуральное число N. Найти и вывести все числа в интервале от 1 до N–1, у которых сумма всех цифр совпадает с произведением цифр данного числа.

Пример: N = 32 → Числа: 24, 15, 6

🛠 Команды
make build - сборка проекта

make run - сборка и запуск

make test - запуск тестов

make debug - отладка в gdb

make analyze - статический анализ кода

make clean - очистка

make rebuild - полная пересборка

make help - справка

🔧 Компиляция вручную

```shell
# Компиляция каждого файла
gcc -Wall -Wextra -std=c17 -Iinclude -c src/main.c -o build/main.o
gcc -Wall -Wextra -std=c17 -Iinclude -c src/number_utils.c -o build/number_utils.o

# Линковка
gcc build/main.o build/number_utils.o -o out/main

# Запуск
./out/main
```

🧪 Тестирование
Проект включает модульные тесты для всех функций. Запуск: make test
